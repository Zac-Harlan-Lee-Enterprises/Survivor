claude-progress.txt — Session continuity log

Format for each entry:
---
[YYYY-MM-DD] <short session label>
Agent: <name>/<model-version>
Branch: <git branch>
DONE   : <comma-separated completed items>
TESTS  : <counts before → after>
NEXT   : <exact next action — file, function, or command>
BLOCKED: <anything requiring human input, or "none">
---

================================================================
[2026-09-08] harness + application bootstrap (/create-ai-harness, new-project mode)
Agent: Claude Code / Claude Fable 5.1
Branch: feat/initial-harness

DONE   : stack scaffold (Vite 8, React 19, TS 6, Tailwind 4, Vitest 5, Playwright 1.63, ESLint 9 + jsx-a11y);
         pure domain (32 teams w/ aliases, zod models, rules engine, pick validation/visibility, idempotent
         results, CSV import + report); demo data layer (deterministic synthetic season, localStorage overlay,
         pinned demo clock w/ subscribe, demo sign-in); connected data layer (single fetch module, OIDC PKCE,
         /public read fallback); all eight experiences (league home, dashboard, pick, leaderboard w/ graveyard,
         season grid + mobile cards, my season, player card, commissioner: players/headshots w/ crop, picks,
         results + manual schedule, settings + champion decision + demo clock, CSV import, audit);
         AWS backend (router, single-table repo, DynamoDB/S3 adapters, presigned uploads, ESPN parser,
         manual provider, idempotent sync, seed script, SAM template); GitHub Pages: base-path helper,
         build-time 404.html + .nojekyll plugin, Pages-parity static server on port 5892;
         harness: init.sh (--with-pages/--stop), agent-status.sh, quality-sweep.sh, policy.sh, hooks
         (pre-commit/commit-msg/pre-push), PreToolUse guard (+AWS/SAM rules, shell-wrapper evasion),
         .claude/settings.json, tests/guardrails, feature_list.json (47 entries), AGENTS.md/CLAUDE.md/
         SECURITY.md, README + docs/*, CI + Pages workflows. Ports moved to 5891/5892/5893 (build/ports.json)
         at the user's request to avoid colliding with PageProof on 4173.
TESTS  : 0 → 124 vitest (domain/config/components/backend/arch/unit) + 28 Playwright (desktop+mobile) +
         20 axe a11y checks + 37 guard self-test probes + 53 guardrail-suite probes. All green.
NEXT   : 1) Human: review + commit the sensitive paths left UNCOMMITTED on purpose (they need a
            SECURITY-REVIEW trailer): .github/workflows/*, infra/*, .claude/settings.json, scripts/hooks/*,
            scripts/lib/policy.sh, harden-github.sh — e.g.
              git add .github infra .claude/settings.json scripts/hooks scripts/lib harden-github.sh
              git commit -m "chore: add CI/Pages workflows, infra and agent guardrails" -m "SECURITY-REVIEW: <your name>"
         2) Human: create the GitHub repo, push feat/initial-harness, open a PR, enable Pages (Source: GitHub
            Actions), then run: bash harden-github.sh <owner>/<repo>
         3) Export the real league spreadsheet to data/import/league.csv and run
              npm run import:report -- data/import/league.csv --out docs/import-report.generated.md
            then import via Commissioner → Import (registry: league-seeded-real-roster).
         4) Connected mode when wanted: docs/aws-connected-mode.md (sam deploy is a human action).
BLOCKED: The spreadsheet screenshot referenced in the brief was NOT attached to the session; no real league
         data was seeded (docs/import-report.md). SAM CLI is not installed locally, so `sam validate --lint`
         (registry: infra-sam-validate) has not been run. No GitHub remote exists yet, so branch protection
         (Layer 3) is unverified.
DECISIONS:
         - HashRouter (zero-404 on Pages) + build-time 404.html shim for clean deep links.
         - Standings are derived, never stored; same validatePick() in browser and Lambda.
         - Provider-independent game ids (<year>-w<ww>-<away>-at-<home>) so provider swaps never orphan picks.
         - Demo schedule is synthetic and flagged; real schedules are never fabricated.
         - Cancelled game → pick void (team returned) by default; missing pick strikes only after the week's
           LAST kickoff; weeks with no schedule data never strike anyone.
         - Public anonymous reads go through /public/* because HTTP API JWT authorizers reject missing tokens.
         - Browser-side crop/resize to two WebP variants; S3 presigned POST policies + finalize HEAD checks.
================================================================

================================================================
[2026-09-08] sensitive-path approval
Approver: Zac Harlan (repo owner / league commissioner)
Reviewed: .github/workflows/ci.yml, .github/workflows/deploy-pages.yml, infra/ (SAM template,
          seed example, samconfig example, README), .claude/settings.json, scripts/hooks/
          (pre-commit, commit-msg, pre-push, guard-destructive-commands.sh), scripts/lib/policy.sh,
          harden-github.sh
Approval:  "the workflow changes look good. please ship them as well" — given in session after being
           shown what was held back and why. Recorded here per the flow in AGENTS.md; the commit
           carries SECURITY-REVIEW: Zac Harlan.
Note:      Repo visibility was also decided by the approver: PUBLIC, with player photos included.
           That publishes nine real people's names and headshots; it was raised explicitly first.
================================================================

--- 2026-09-09  Claude (feat/add-tracy-nelson) ---
DONE   : Tracy Nelson added (SEA, week 1 — same team as Sheila Acker); Maya Israel's photo
         re-imported; import-headshots.mjs now reads the roster from the generator, not the
         built fixture, removing the generate-import-generate ordering trap; roster counts in
         specs derived from the fixture instead of hard-coded. Then: /rules page rendering
         docs/survivor-rules.md in-app, linked from the primary nav (desktop + mobile) and the
         footer, with a live-settings strip; begin/end-player-rules markers keep engineering
         notes off the page; small Markdown subset renderer in src/lib/markdown.ts.
         Fixed two latent bugs: the mobile nav's hard-coded grid-cols ternary, and
         quality-sweep.sh mangling registry verifications through jq @tsv.
TESTS  : vitest 166 -> 184 passing; playwright 66 -> 71 passing (2 skipped: the footer link is
         desktop-only). quality-sweep.sh clean.
NEXT   : Open/merge the PR for feat/add-tracy-nelson.
BLOCKED: Merging still needs a branch-protection change only the repo owner can make, in the
         GitHub UI under Settings -> Branches -> main: turn OFF "Require approval of the most
         recent reviewable push" and "Require review from Code Owners". Those two, plus "Do not
         allow bypassing the above settings" and a single maintainer, leave nobody able to merge.
NOTE   : The PreToolUse guard blocks any command whose TEXT contains a mutating gh api call,
         including documentation quoting one. Describe such fixes in prose, not as a pasteable
         command, or the note cannot be written.

--- 2026-09-09  Claude (feat/add-bradley-riedell) ---
DONE   : Bradley Riedell added (DET, week 1 — third player on the Lions, with Dave Johnson and
         James Parker). Photo imported from photos/BradleyRiedell.jpeg. Roster now 11.
TESTS  : vitest 184 passing, playwright 71 passing / 2 skipped, quality-sweep clean. No spec
         needed editing: the fixture-derived roster size from the previous session held.
NEXT   : Open/merge the PR for feat/add-bradley-riedell.
BLOCKED: none.

--- 2026-09-09  Claude (feat/week-selector) ---
DONE   : Header week chip is now a select listing all 18 weeks; the league home slate follows it
         and names the teams on bye for that week. Selection lives in the URL (?week=N), so it is
         shareable and reload-safe, with a fallback to the current week for a bad value. The hero
         still reports the live week. New pure domain fn teamsOnBye (derived from the schedule,
         never stored). WeekSelect renders in the header on desktop and beside the slate heading
         on mobile, so exactly one is visible at any width.
TESTS  : vitest 189 passing (5 new for teamsOnBye), playwright 81 passing / 2 skipped (5 new in
         week-select.spec.ts), quality-sweep clean, npm run validate green.
NEXT   : Open/merge the PR for feat/week-selector.
BLOCKED: none.
NOTE   : Playwright TRIMS a hasText string, so filter({hasText: ' at '}) also matched "Patriots"
         in the bye list and inflated a count by one. Use a regex (/\sat\s/) when the surrounding
         whitespace is the point.

--- 2026-09-09  Claude (feat/add-five-players) ---
DONE   : Added Chloe Bourque (JAX), Jared Marks (LAC), Joseph Tomczuk (JAX), Corey Cowell (PIT),
         Melanie Moeller (LAC), Jason Snook (JAX), Matt Hadley (GB), Mike Lancaster (SEA) and
         KC Walker (JAX). League is 20. Eight photos imported; Chloe Bourque is the only player
         without one and falls back to the neutral default avatar.
         Relaxed the 'headshots-real-photos' registry check, which asserted every profile had an
         image — stricter than its own description and a blocker for any new player before their
         photo arrives. It now asserts every image is a real WebP and no profile points at a
         missing image; proved by injecting a dangling reference.
TESTS  : vitest 189 passing, playwright 81 passing / 2 skipped, npm run validate green,
         quality-sweep clean. No spec needed editing (roster size is fixture-derived).
NEXT   : Open/merge the PR. Branched off feat/week-selector, so merge PR #5 first.
BLOCKED: none.

--- 2026-09-09  Claude (feat/add-paul-lim) ---
DONE   : Added Paul Lim (JAX) and Tony Canody (LAC), both with photos. League is 22. Chloe
         Bourque is the only player without a photo and falls back to the default avatar.
         Tony needed a CROP_NUDGES entry (y: 0) — his caricature's head fills the frame, so the
         attention crop sliced the top off.
TESTS  : vitest 193 passing, playwright 81 passing / 2 skipped, validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.

--- 2026-09-09  Claude (feat/live-scores) ---
DONE   : Live scores on the league slate. The ESPN pipeline already existed but its ONLY trigger
         was the commissioner Results tab, which <Writable> removes from the published build —
         so GitHub Pages had never refreshed a score. Its poll also armed only on in_progress,
         the very fact a sync exists to discover, so it could not start unaided.
         New: src/domain/rules/live.ts (weekIsLive, changedScores — pure, no timers),
         src/app/useLiveScores.ts (polls for every reader, kickoff-driven, pauses on a hidden
         tab, silent on failure), GameRow shows in-progress scores + "3rd 5:21" + a one-second
         pulse on the number that moved. Live clock rides on SyncSummary.liveDetail and is
         NEVER persisted (it would bump resultVersion every tick or go stale between syncs).
         Fixed on the way: the slate ignored applyGameOverrides (commissioner corrections were
         invisible); ResultsPanel printed raw enums at people; syncResults no longer audits a
         no-op poll.
TESTS  : vitest 224 passing (live.test.ts, GameRow.test.tsx, espn + sync additions);
         playwright 89 passing / 2 skipped incl. new live-scores.spec.ts. Sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.
NOTE   : e2e MUST keep ESPN stubbed. resetDemo now aborts every call to the feed; without it the
         suite fires real requests that hang, once per page view, doubled by retry: 1.

--- 2026-09-09  Claude (feat/add-joanna-moss) ---
DONE   : Added Joanna Moss (DAL) with her photo — the first and only player on the Cowboys.
         League is 23. Chloe Bourque remains the only player without a photo.
TESTS  : after merging main (live scores): vitest 224 passing, playwright 89 passing / 2 skipped,
         validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.

--- 2026-09-09  Claude (feat/add-allison-petty) ---
DONE   : Added Allison Petty (DET) with her photo — fourth on the Lions. League is 24. Chloe
         Bourque remains the only player without a photo.
TESTS  : vitest 224 passing, playwright 89 passing / 2 skipped, validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.

--- 2026-09-09  Claude (feat/add-don-turner) ---
DONE   : Added Don Turner (PIT, second on the Steelers) and Cindy Mendoza (JAX, seventh on the
         Jaguars). League is 26. His square source had no
         crop slack, so CROP_NUDGES gained an optional `zoom` (keep a smaller square centred on
         a point). x/y were unified to mean the crop CENTRE as a fraction of the image; the two
         existing entries were re-expressed and proved byte-identical. Don's medium variant is
         200px, not 512 — zooming cannot invent detail.
TESTS  : vitest 226 passing, playwright 89 passing / 2 skipped, validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.
NOTE   : cropNudges.test.ts caps the list at 3 entries and it is now full. A fourth is a
         deliberate decision, not a routine edit — prefer a better original.

--- 2026-09-09  Claude (feat/add-collins-childers) ---
DONE   : Added Phyllis Collins (SEA) and Wesley Childers (DET), both with photos. League is 28.
         Neither needed a crop nudge (list still at its cap of 3). Phyllis's original is only
         239x231 and dark, so her headshot is 231px — the dark edges are in the source, not the
         crop. Chloe Bourque remains the only player with no photo at all.
TESTS  : vitest 226 passing, playwright 89 passing / 2 skipped, validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.

--- 2026-09-09  Claude (feat/league-message) ---
DONE   : New LeagueMessage section between the hero and Still standing — a commissioner's note
         to the whole league. Copy is editorial, keyed by week in NOTES; an unwritten week
         renders nothing, so it cannot go stale. Week 1 note written.
TESTS  : vitest 226 passing, playwright 93 passing / 2 skipped (new league-message.spec.ts
         asserts DOM order hero -> note -> standings), validate green.
NEXT   : Make the note commissioner-editable (settings panel + fixture field) if it is wanted
         every week; right now writing one is a code change.
BLOCKED: none.

--- 2026-09-10  Claude (feat/chloe-bourque-photo) ---
DONE   : Imported Chloe Bourque's photo — the last player on the default avatar — and added
         Tina Bush (DET). League is 29, all with real headshots. No crop nudge needed.
         Open questions for the commissioner: her photo file is Tina_Busch.jpeg vs the roster's
         "Tina Bush"; and that source is only 116x116, so her headshot is softer than the rest.
         She was also added AFTER the week 1 deadline, with the DET game already in progress.
TESTS  : vitest 226 passing, playwright 93 passing / 2 skipped, validate green, sweep clean.
NEXT   : Open/merge the PR.
BLOCKED: none.

--- 2026-09-14  Claude (feat/week1-review-note) ---
DONE   : Replaced the commissioner's note with a week 1 review, researched against the live ESPN
         scoreboard plus contemporary reports (Cardinals 26-14 at SoFi, Lawrence 4 TD / 24-0
         half, Lions 31-30 OT decided by a failed two-point try). Mapped to weeks 1 AND 2: week
         1 is not final until the Monday night game syncs, so a week-2-only key would show a
         preview of games already played.
         League after week 1: 21 unmarked; 8 down to two lives (the six on LAC, Joanna on DAL,
         Matt on GB). Nobody eliminated.
TESTS  : vitest 226 passing, playwright 93 passing / 2 skipped, validate green, sweep clean.
NEXT   : Write the week 2 note once week 2 picks are in; remove the week 1 key then.
BLOCKED: none.

--- 2026-09-15  Claude (feat/funnier-commish-note) ---
DONE   : Rewrote the week 1 note for comedy (facts unchanged, all still checked). Replaced the
         closing joke, which the commissioner reported did not land, with one resting on a
         verified fact: SEA-NE 13-10 was week 1's lowest-scoring game, 23 points, ten clear of
         the next. Added a gold CALLOUT for the week 2 instruction — DM picks on Teams, lock at
         7:10 PM Thursday (5 min before DET at BUF, the deadline the engine enforces). It is set
         apart from the banter because an instruction buried in jokes gets skimmed.
TESTS  : vitest 226 passing, playwright 95 passing / 2 skipped (new callout assertions),
         validate green, sweep clean.
NEXT   : Write the week 2 review once week 2 is played; drop the week 1 key then.
BLOCKED: none.

--- 2026-09-17  Claude Opus 5 (feat/week2-picks) ---
DONE   : Recorded week 2 picks. 27 of 29 answered over Teams and Outlook; each carries its real
         submission date (day granularity is all that was supplied, so times are 15:00Z). The
         other 2 are explicit `2: null` in STORY — asked and silent, not merely unrecorded.
         Picks: SF 9 (Shahid, James, Stacey, Dominic, Tracy, Jason, Matt, Mike, Cindy), TB 7
         (Nate, Sheila, Chloe, Joseph, Corey, KC, Paul), BAL 3 (Maya, Dave, Don), CHI 1
         (Bradley), CLE 1 (Tony), BUF 3 (Allison, Phyllis, Joanna), PHI 1 (Melanie). SF is 11 with
         Wesley and Zac — nearly half the league on one team.
         Note Tony on CLE is opposite the seven on TB in the same game — that one settles both.
         Allison and Phyllis on BUF ride the Thursday opener, so theirs lock first.
         All 27 checked against the real schedule: no reuse of a week 1 team, no byes, no dupes.
         Source is 'commissioner', not 'player': these were transcribed, not entered in the app.
FIXED  : Seeded submittedAt could land AFTER the week's deadline — 20 of 29 week 1 picks did.
         The generator anchored on each pick's own game kickoff, but the deadline is 5 min
         before the week's FIRST kickoff, so anyone on a late Sunday or Monday game got an
         impossible timestamp. Now anchored on the week's first kickoff. Caught by the new test,
         not by eye.
ADDED  : src/data/demo/fixtures.test.ts — the invariants validatePick would have enforced had
         these arrived through the app, applied to the seed instead: every pick resolves to a
         real game that week in which the team plays, no player reuses a team or double-picks a
         week, and nothing is submitted after its deadline. Guards future weeks, not just this
         transcription.
TESTS  : vitest 238 passing (12 new across fixtures.test.ts, reveal.test.ts and seed.test.ts),
         playwright 95
         passing / 2 skipped, validate green, sweep clean, init.sh HTTP 200. Registry: demo-fixtures-deterministic, real-schedule and
         league-seeded-real-roster all re-verified; updated the last one's stale description
         (it still claimed nine players and a synthetic schedule).
CHANGED: In LOCAL commissioner mode the commissioner now sees every pick immediately —
         everywhere, not just the admin panel. Asked for twice: the panel default first, then
         the grid, which still showed dots. The gate is the one the Layout banner already uses
         (demo AND not read-only), threaded as ctx.revealAllPicks from createServices — the
         single place modes are chosen — into the demo adapter's getSeasonSnapshot/listPicks.
         The domain redactSnapshot is UNTOUCHED, so connected mode still redacts on the server
         where no browser flag can reach it, and the published read-only build still conceals.
         Players never see rivals' picks in any mode. The panel toggle still hides on demand.
         This deliberately narrows a safeguard that a past regression test protected, so both
         sides are pinned in src/data/demo/reveal.test.ts and the e2e regression was retargeted
         from the commissioner to a plain player, which is the case that must hold everywhere.
         Note the panel computes `commissionerIsCompeting` and uses it only for wording, never
         for the concealment decision; that asymmetry is unchanged but worth a look if
         revisited.
FIXED  : Signed out now counts as the operator in local mode. The reveal originally required
         isCommissioner, and a session is exactly what a reseed clears — so the commissioner
         refreshed, saw dots, and reported "still not seeing them" three times while the data
         was correct the whole way. Two bugs wearing one costume: the session was being dropped
         (below) AND the state it dropped into was the one that sees least. Signing in as a
         PLAYER still conceals, so demo mode can still answer "what does she see?" honestly.
         LESSON for the next session: Playwright starts with an empty browser and signs in
         explicitly, so it never reproduced the reported state. When a user says they cannot
         see something, reproduce THEIR browser — stale storage, signed out — before trusting
         a green driver run.
FIXED  : A reseed signed the viewer out. The session lives inside the overlay blob, so
         resetDemoStorageIfStale wiped it along with the stale data — and since a signed-out
         viewer sees the least, regenerating the fixture looked exactly like the new picks had
         failed to load. Reported as "still not seeing them" twice before the cause was found,
         which is the tell: it was never the data, it was who the browser thought you were.
         resetDemoStorageIfStale now returns the previous sessionPlayerId and DemoStore carries
         it into the first load. An explicit reset() still signs out — that is what it is for.
VERIFIED: Drove the running app with Playwright (sign in as commissioner → /commissioner/picks
         → week 2) and read the rendered rows: every pick shows with the right team and logo,
         and the silent ones show "no pick". Screenshots in the session scratchpad.
NEXT   : Write the week 2 commissioner note once week 2 is played, and drop the week 1 key.
         Only Jared Marks and Tina Bush are still silent; each loses a life at the 00:10Z
         Thursday deadline if they stay that way. Worth a nudge before then.
BLOCKED: none. Branch is local; pushing and the PR are yours to approve.

--- 2026-09-17  Claude Opus 5 (feat/week2-picks, cont.) ---
DONE   : Replaced the week 1 review with a week 2 PREVIEW, written for Thursday's deadline.
         Keyed to weeks 1 and 2 as before: week 1 has no recorded result so the engine still
         calls it current, but every week 1 game has been played.
         Every counted claim is drawn from the fixture and the real cached schedule — no byes
         (32 teams playing), 11 on SF, 7 on TB against Tony alone on CLE in that one game, 3 on
         BUF in the Thursday opener, 25 of 27 on home teams, the week 1 burn counts, Allison
         picking against the Detroit team that saved her, Joanna and Matt both back with the
         herd after being punished for going it alone.
ADDED  : tests/unit/leagueMessageFacts.test.ts — 12 assertions, one per counted claim, checked
         against the season the note describes. When a pick lands and "eleven of you" becomes
         twelve, these fail and the note gets rewritten. That is the signal working. It reads
         the fixture from disk like cropNudges.test.ts next door, because tests/unit compiles
         under tsconfig.node.json which does not include src — and the architecture rules
         forbid anything under src/features importing a data adapter, test file or not.
CHANGED: The league-message e2e no longer asserts on a specific joke (/jaguars/i), which would
         need editing every week. It asserts the section renders with body copy; the facts are
         the unit test's job and the pick instruction keeps its own dedicated test.
TESTS  : vitest 250 passing (12 new), playwright 95 passing / 2 skipped, validate green, sweep
         clean.
NEXT   : Write the week 2 REVIEW once week 2 is played, and drop the preview then. Jared Marks
         and Tina Bush are still silent — the note now names them, but a nudge before 7:10 PM
         Thursday is worth more than a paragraph they may not read.
BLOCKED: none. Branch is local; pushing and the PR are yours to approve.

--- 2026-09-18  Claude Opus 5 (feat/week2-picks, cont.) ---
DONE   : Recorded the last two week 2 picks — Jared Marks on SF, Tina Bush on TB — taking the
         league to 29 of 29. STORY now has no `2: null` entries.
         IMPORTANT: these were relayed on Friday morning, 8h46m AFTER the 7:10 PM Thursday lock,
         and after DET at BUF had been played. Asked the commissioner before recording, because
         a pick made with Thursday's result already public is exactly what the shared deadline
         exists to prevent, and because the alternative cost two real people a life each. He
         confirmed both were SENT before the lock and only relayed late, so they are recorded
         as on-time at the usual 15:00Z day-granularity, no override, no life lost. If that
         confirmation was wrong the fix is a commissioner override with a reason, NOT a
         backdated submittedAt — the fixture test would catch the latter anyway.
CHANGED: Updated the note's numbers only, per instruction — prose and jokes untouched. SF
         eleven → twelve, TB seven → eight, the CLE-at-TB game eight → nine, home teams
         twenty-five of twenty-seven → twenty-seven of twenty-nine. The callout had to change
         beyond a number: it named Jared and Tina as missing, which is now false, so it reports
         the clean sweep and carries the same obligation forward to week 3.
         leagueMessageFacts.test.ts caught all four stale claims by name before I touched
         anything — the guard doing exactly its job one commit after being written.
         The league-message e2e no longer pins the callout to "week 2"; it asserts the three
         things that must survive every rewrite: where to send, when it locks, what missing
         costs.
TESTS  : vitest 251 passing (13 fact assertions, one new for the lock time), playwright 95
         passing / 2 skipped, validate green, sweep clean.
NEXT   : Week 2 is under way and DET at BUF is played, but no result is recorded — results come
         from the live ESPN sync, so the standings still show 29 alive at 3/3. Write the week 2
         review once the slate is final, and drop the preview then.
BLOCKED: none. PR #17 is open; merging stays with the commissioner.

--- 2026-09-18  Claude Opus 5 (feat/week2-picks, cont.) ---
DONE   : Removed the note's callout at the commissioner's request. Week 2 is locked with all 29
         picks in, so there is nothing for anyone to act on, and an instruction shown when none
         is owed is how a real one gets ignored later. The note now ends on the spent-teams
         paragraph. `callout` stays optional on the note type and the component still renders it
         set apart — only this week's copy went.
NEXT   : THE CALLOUT COMES BACK with the week 2 review, once the slate is final and week 3 is
         the thing to chase. Two guards are waiting for it and both are designed to fail then,
         which is the reminder:
           - tests/e2e/league-message.spec.ts 'shows no pick instruction while the week is
             locked' — its comment carries the three assertions to restore verbatim (where to
             send, when it locks, what missing costs). Restore them; do not delete the test.
           - tests/unit/leagueMessageFacts.test.ts — the two retired assertions ("all twenty-nine
             picks are in", "they locked at 7:10 PM Thursday") go back alongside whatever the
             new callout claims.
         Also still open: no week 2 result is recorded (DET at BUF has been played), so
         standings show 29 alive at 3/3 until the live ESPN sync lands.
BLOCKED: none. PR #18 open; #17 already merged. Merging stays with the commissioner.

--- 2026-09-18  Claude Opus 5 (fix/week2-missed-picks) ---
DONE   : REVERSED the earlier call on Jared Marks and Tina Bush. The commissioner first said
         both had sent before the 7:10 PM Thursday lock and were only relayed late; he has now
         decided they missed the cut-off. Both are back to `2: null` for week 2 — recorded as
         misses, not picks. Week 2 is 27 of 29.
         Note numbers follow: SF twelve → eleven, TB eight → seven, the CLE-at-TB game nine →
         eight, home teams twenty-seven of twenty-nine → twenty-five of twenty-seven. Prose
         untouched, callout still absent.
VERIFIED: The engine does NOT yet show the lives lost, and that is not a bug. `currentWeek` is
         the first week whose phase is not 'final' (engine.ts ~295). No week 1 result is
         recorded, so week 1 is still current, week 2 is forced to 'upcoming' (~304), and every
         week 2 outcome is 'pending' — nobody can be struck by a deadline in a week the engine
         considers unplayed. Simulated week 1 going final and re-evaluated: both resolve to
         'missing' with consumedLife true. So the miss is correctly RECORDED now and will BITE
         when the live ESPN sync lands week 1's results. Standings will keep showing 29 alive at
         3/3 until then — expect a question about it.
NEXT   : The note does not mention the two misses. Prose was left alone deliberately; the
         commissioner may want a line about it in the week 2 review, where the callout also
         returns (see the two waiting guards in the previous entry).
BLOCKED: none. PR #19 stacks on #18, which stacks on merged #17. Merge in order.

--- 2026-09-21  Claude Fable 5.1 (feat/add-craig-mowers) ---
DONE   : Added Craig Mowers. Week 1 on the Chargers, week 2 `null` — he sent no pick, so it is
         a miss like Jared's and Tina's, minus the late-answer history. The league is thirty;
         week 2 is 27 of 30. His photo was already in photos/ ("Craig Mowers headshot.png",
         498x619); the importer matched it by name and the attention crop is centred on his
         face, so no CROP_NUDGES entry. No other headshot changed.
         He is the SEVENTH Charger in week 1, which made two of the note's claims false, so
         the numbers moved and nothing else: "Jacksonville and the Chargers gone for seven of
         you apiece, Detroit for six, Seattle for four", and the commissioner is now "one of
         the seven buried by the Chargers". Week 2 counts (11 SF, 7 TB, 8 in CLE-at-TB, 25 of
         27 at home) are untouched because he has no week 2 pick.
VERIFIED: npm run validate green; npm run test:e2e 95 passed / 2 skipped; bash init.sh → 200 on
         5891; quality-sweep exits 0. leagueMessageFacts pins the three misses by name and LAC
         at seven; fixtures.test pins thirty profiles. feature_list.json
         league-seeded-real-roster description updated (30 players, 27 of 30) and its
         verification re-run.
NEXT   : Unchanged from the previous entry — no week 1 or week 2 result is recorded, so all
         thirty still show 3/3 and the three misses bite only when results land. The week 2
         review should bring the callout back (two guards are waiting) and may want a line
         about the misses; week 3 picks will need collecting.
BLOCKED: none. Merging stays with the commissioner.

--- 2026-09-22  Claude Fable 5.1 (feat/week2-review-note) ---
DONE   : Rewrote the commissioner's note as the WEEK 2 REVIEW, chasing week 3. Every fact was
         checked against the ESPN scoreboard and the ESPN game recaps on 2026-09-22 (not
         recalled): SF 35-13 with TDs on the first five drives and Purdy 20 of 22; CLE 23-19 at
         TB after a 2h12m lightning delay, Whiteheart go-ahead TD, Mayfield 4th-down incomplete
         from the 20, TB's 9 first-half points all field goals; NO 24-17 at BAL on a Shough
         4th-down sneak at 1:28 after BAL led 14-3 at the half, replay "could not prove he did
         not break the plane"; MIN 9-3 at CHI with no TDs, Swift fumble at the 1, blocked 23-yd
         FG; PHI 24-20 at TEN on a 3-yd TD with 9 seconds left (Cooper's first career TD, turf
         157F); BUF 41-31, Allen 5 total TDs, 21-0 with 11:48 left in Q2, first game at the new
         stadium; LAC lost 26-14 twice (ARI, LV), Harbaugh 0-2 first time college or pro;
         CAR 34-3 at ATL. League math: 14 lost a life in week 2 (7 TB, 3 BAL, Bradley CHI, 3
         misses); 10 at three lives, 17 at two, 3 at one (Nate, Jared, Craig); nobody out.
         The CALLOUT IS BACK: DM on Teams, locks 7:10 PM Thursday (Central; 5 min before ATL at
         GB, 2026-09-25T00:15Z), missing costs a life. Note is keyed to weeks 1, 2 AND 3.
         tests/unit/leagueMessageFacts.test.ts now pins every week 1 and 2 final (keyed by the
         seed's game ids) and derives lives from picks + finals, so the note's outcome claims
         are asserted, not just its pick counts. The e2e guard's three callout assertions are
         restored ('tells everyone where to send a pick, when it locks, and what missing costs').
TRIED  : Seeding real results into the fixture via `npm run schedule:fetch` (the cache carries
         scores) + fixtures:generate. Engine then gives the right standings, but the whole e2e
         baseline assumes "Wednesday before week 1, nothing kicked off, everyone 3/3", and
         reveal/sync unit tests assume a seed with no finals — 4 unit failures before e2e.
         REVERTED; out of scope for a note change.
NEXT   : STANDINGS STILL SAY 30 AT 3/3 under a note that says fourteen lost a life. Pre-existing
         (see 09-18 entry) and now visible on one screen. useLiveScores polls only while a week
         is live, so on the published site weeks 1-2 never land unless a browser had the page
         open during games. Fix options: (a) sync every non-final week whose games have kicked
         off, once, on load; (b) seed results and move the e2e baseline forward. (a) is smaller.
         Week 3 picks need collecting by 7:10 PM Thursday 9/24.
BLOCKED: none. Merging stays with the commissioner.

--- 2026-09-22  Claude Fable 5.1 (feat/week2-review-note, cont.) ---
CORRECTION: THE STANDINGS ALREADY AGREE WITH THE NOTE ON THE PUBLISHED SITE. The "results never
         land on Pages" belief (09-18 entry, and my own NEXT above) came from headless probes.
         ESPN's scoreboard answers 403 from Akamai, with no access-control-allow-origin, to
         every headless engine (Playwright chromium old/new headless, channel 'chrome' headless,
         curl with a Chrome UA). Plain curl and a HEADED Chrome get 200 with ACAO *. Verified in
         headed Chrome against the live site today: it fetched week 1, then week 2, the hero
         moved to WEEK 3, and the standings read 10 at three lives / 17 at two / 3 at one —
         exactly the note's numbers. The existing useLiveScores poll chains through played
         weeks on load: a kicked-off game still marked scheduled counts as live, and
         currentWeek advances after each sync. NO CODE CHANGE NEEDED. A catch-up hook was built,
         found redundant, and thrown away unpushed.
VERIFIED: chromium.launch({ channel: 'chrome', headless: false }) is the only way to see the
         feed work from a script. CI and headless probes will always show 3/3 — do not chase it.
NEXT   : Week 3 picks by 7:10 PM Thursday 9/24; then the week 3 review. Nothing else open.
BLOCKED: none. PR #21 awaits the commissioner's merge.

--- 2026-09-24  Claude Opus 5.5 (feat/week3-picks-note) ---
DONE   : Recorded the first nine week 3 picks (7 KC, Jared BUF, Joanna SEA; Zac on KC) with the
         real dates they arrived. Note rewritten as the week 3 preview.
RULE   : A PREVIEW NEVER REVEALS THIS WEEK'S PICKS — not who, not what, not counts per team. The
         commissioner hides picks until the week's first kickoff and a note that hints at them
         undoes the deadline. A first draft did exactly that and was rejected. The preview now
         talks only about the slate and past weeks; a test in leagueMessageFacts fails if any
         player's first name, or "picked" / "are on" / "is on", appears in WEEK_3_PREVIEW.
VERIFIED: validate green; e2e 95 passed; sweep clean. Facts checked today: KC 2-0, MIA 13 points
         twice, SEA 2-0 and Super Bowl LX champions, Walker (SB LX MVP) now a Chief, WAS 0-2 and
         opening at home, LAC 26-14 twice, ATL lost 34-3 to CAR, GB beat NYJ by 3.
NEXT   : Remaining week 3 picks as they arrive (no note changes needed — it names nobody). Week 3
         review after Monday night, when picks are public.
BLOCKED: none. Merging stays with the commissioner.

--- 2026-09-24  Claude Opus 5.5 (feat/week3-more-picks) ---
DONE   : Three more week 3 picks: Jason Snook DET (vs NYJ), Tracy Nelson KC (at MIA), Nate Adams
         SF (vs ARI). None reuses a team. Twelve of thirty in. Note untouched — it names nobody.
VERIFIED: validate green; sweep clean.
         Then Mike Lancaster KC, James Parker SEA (at WAS), Matt Hadley KC, Paul Lim KC.
         Then Dominic Green KC, Shahid Ali KC, Joseph Tomczuk PHI (at CHI, Monday), Sheila Acker KC, Dave Johnson KC, Don Turner SEA, Melanie Moeller SF. Twenty-three of thirty.
NEXT   : Remaining picks before 7:10 PM tonight.

--- 2026-09-24  Claude Opus 5.5 (feat/week3-stragglers) ---
DONE   : Week 3 stragglers, after #23 merged with 23 of 30. Chloe Bourque KC, Tony Canody KC, Tina Bush KC, Corey Cowell SEA. Twenty-seven of thirty.
NEXT   : Still owed before 7:10 PM tonight: Allison Petty, Phyllis Collins, Wesley Childers.

--- 2026-09-24  Claude Opus 5.5 (feat/week3-last-picks) ---
DONE   : After #24 merged with 27 of 30. Wesley Childers KC. Twenty-eight of thirty.
NEXT   : Still owed before 7:10 PM tonight: Allison Petty, Phyllis Collins.
